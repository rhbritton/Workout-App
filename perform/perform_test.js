'use strict';

describe('myApp.perform module', function() {

	beforeEach(module('myApp.perform'))

	describe('perform controller', function() {

		it('should ....', inject(function($controller) {
			//spec body
			var performCtrl = $controller('PerformCtrl')
			expect(performCtrl).toBeDefined()
		}))

	})
})