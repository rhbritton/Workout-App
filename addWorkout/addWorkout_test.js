'use strict';

describe('myApp.addWorkout module', function() {

	beforeEach(module('myApp.addWorkout'))

	describe('addWorkout controller', function() {

		it('should ....', inject(function($controller) {
			//spec body
			var addWorkoutCtrl = $controller('AddWorkoutCtrl')
			expect(addWorkoutCtrl).toBeDefined()
		}))

	})
})